import 'package:flutter/material.dart';

class HomePrncipal extends StatelessWidget {
  const HomePrncipal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}